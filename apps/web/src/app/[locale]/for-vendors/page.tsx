import { ForVendorsContent } from "./for-vendors-content";

export const metadata = {
  title: "List your business — Ceylon Weddings",
  description: "Reach couples planning their Sri Lankan wedding. Free listing, WhatsApp leads, zero commissions.",
};

export default function ForVendorsPage() {
  return <ForVendorsContent />;
}
