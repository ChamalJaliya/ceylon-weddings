# Ceylon Weddings site

Single public Next.js app (`@ceylonweddings/site`) on port 3000.

- Marketing and vendor marketplace
- Couple / family planning at `/planning`
- Vendor pro at `/pro`
- Admin at `/admin`
- Guest wedding pages at `/w/[slug]`

API: `http://localhost:4000` (`/docs` for Swagger).
