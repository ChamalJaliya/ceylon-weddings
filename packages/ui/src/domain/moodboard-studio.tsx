"use client";

/**
 * Studio chrome lives in the web app moodboard package (Konva engine).
 * This export keeps the domain kit surface for layout wrappers.
 */
export { MoodboardStudioShell } from "./moodboard-studio-shell";
